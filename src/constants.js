/**
 * @enum
 */
const MessageTypes = {
  RECONNECT: 'reconnect',
  CONNECT: 'connect',
  DISCONNECT: 'disconnect',
  DISPATCH: 'dispatch',
  RESPOND: 'respond',
  TIME: 'time',
  TERMINATE: 'terminate',
  SETUP: 'setup',
  CHECK: 'check'
};


module.exports = {
  MessageTypes
};
